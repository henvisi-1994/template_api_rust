services:
  api:
    build: .
    container_name: template_api_rust
    ports:
      - "3000:3000"
    env_file:
      - .env
    restart: unless-stopped

    # === OPCIONAL: Descomenta estas líneas si quieres un Postgres local para desarrollo ===
    # depends_on:
    #   - db
    # networks:
    #   - app_network

# === OPCIONAL: Base de datos PostgreSQL local (solo para desarrollo) ===
# db:
#   image: postgres:16-alpine
#   container_name: template_postgres
#   environment:
#     POSTGRES_USER: postgres
#     POSTGRES_PASSWORD: postgres
#     POSTGRES_DB: template
#   ports:
#     - "5432:5432"
#   volumes:
#     - postgres_data:/var/lib/postgresql/data
#   restart: unless-stopped
#   networks:
#     - app_network

# volumes:
#   postgres_data:

# networks:
#   app_network:
#     driver: bridge